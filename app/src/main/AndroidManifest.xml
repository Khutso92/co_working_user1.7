<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.khutsomatlala.hackaton_user11">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/backlogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/backlogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Auth_login"
            android:label="Co-Working Spaces">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".AuthActivity" />
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".book_new"
            android:label="Booking" />
        <activity android:name=".Splash" />
        <activity
            android:name=".MainActivity"
            android:label="Co-Working Spaces" />
        <activity android:name=".MaxPicActivity" />
        <activity
            android:name=".PlaceDetailsActivity"
            android:label="@string/title_activity_place_details"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".ReviewActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".Time_picker" />
        <activity android:name=".Read_more" />
        <activity android:name=".Forgot_Password"></activity>
    </application>

</manifest>